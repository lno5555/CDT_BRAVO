This script configures DNS for Windows and Linux boxes, and adds different features like a firewall, an additional user and query logging.

you'll need the inventory file and to possibly change the IP's and DNS servers as you see fit, then run the playbook
